package com.phantom.scroll.gesture

/** Vertical swipe direction. */
enum class ScrollDirection {
    /** Finger moves bottom→top: content scrolls up / next page. */
    UP,

    /** Finger moves top→bottom: content scrolls down / previous page. */
    DOWN
}

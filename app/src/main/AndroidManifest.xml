<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Foreground service: keeps the accessibility process at foreground priority so its status
         notification stays resident (= process alive, user can detect liveness) and the system is
         far less likely to reclaim it under memory pressure. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".PhantomScrollApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhantomScroll">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PhantomScroll">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.PhantomScrollService"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Independent foreground service for keep-alive (mirrors gkd's StatusService). Started from
             PhantomScrollService.onServiceConnected while the accessibility binding counts as a
             foreground component, so its startForegroundService() takes the foreground-start
             exemption path. Holds the resident status notification that keeps the process at
             foreground priority and signals liveness to the user. -->
        <service
            android:name=".service.KeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Display the running state of the PhantomScroll accessibility auto-scroll service so it is not reclaimed by the system." />
        </service>

        <!-- Statically registered BroadcastReceiver for status bar notification controls.
             This allows the notification buttons to wake up/pull up the app process even if
             it has been killed by the task manager. -->
        <receiver
            android:name=".service.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.phantom.scroll.ACTION_TOGGLE" />
                <action android:name="com.phantom.scroll.ACTION_STOP" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
